{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": ["-config", "config.yaml"],
            "env": {
                "PATH": "${env:PATH};${workspaceFolder}"
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}